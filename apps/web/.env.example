# AES web client — copy to .env.local for local dev (Vite reads VITE_* vars).
# In dev, requests to /api are proxied to http://localhost:3000 (see vite.config.ts),
# so the default base URL is the relative /api prefix.
VITE_API_BASE_URL=/api
